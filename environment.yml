name: diplom
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.9
  - numpy
  - pandas
  - matplotlib
  - ipykernel
  - google-cloud-bigquery
  - pyarrow=14.0.1  # Ensure compatibility with BigQuery
  - tqdm
  - pip